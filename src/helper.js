const emailer = require('../emailer');


module.exports = {

    isActividad = (subject,text,receiver,activity) =>{
        if(activity){
            //emailer(subject,text,receiver);
        }else return;
    }

}